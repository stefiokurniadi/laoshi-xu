/** Stable identity for homepage quiz when not signed in (full `/api/word` flow, no DB writes). */
export const GUEST_HOME_QUIZ_USER_ID = "guest-quiz-home";

/** Local-only persisted score for guest homepage quiz (separate from demo trial keys). */
export const GUEST_HOME_QUIZ_SCORE_KEY = "laoshi-xu:anon-home-quiz:score";
